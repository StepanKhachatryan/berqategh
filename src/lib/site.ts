/**
 * Where the site lives.
 *
 * One constant, read by the page that shows the address and by the script that
 * draws the QR code, so the code can never end up pointing somewhere the site
 * no longer is. Change it here and run `npm run qr`.
 */
export const SITE_URL = 'https://berqategh.am/';

/** The same thing as people say it out loud. */
export const SITE_HOST = 'berqategh.am';
